apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-chat
  namespace: chat
  annotations:
    kubernetes.io/ingress.class: "nginx"
    # Forceer geen HTTPS redirect
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    # Verleng de time-outs naar 3600 seconden (1 uur) om te voorkomen dat lange chat-verbindingen afbreken
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
spec:
  rules:
  - host: chat.festivalstream.nl
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: chat-svc
            port:
              number: 80